import React from "react";
import QuoteForm from "../components/QuoteForm"; // adjust the path if needed

const Quote = () => {
  return (
    <div className="page-wrapper">
      <QuoteForm />
    </div>
  );
};

export default Quote;
